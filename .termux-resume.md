# Resumo · continuação da sessão (termux fechou)

> Este arquivo registra o estado do trabalho para retomar caso o terminal feche.
> Atualizado: 20/09/2026 · sessão de hoje CONCLUÍDA e commitada (release 4.9.0)

## Projeto
- Repo: `/root/pulsa` (branch `main`)
- App Android Kotlin/Gradle: `app/`
- Último commit: `Pulsa 4.9.0 (97)` — Nova estrutura hub + rádio Virgin/Victor + painel admin de banir

## O que foi feito na release 4.9.0
1. **Estrutura nova (hub)**: o app abre na Home da Virgínia (tela cheia, sem barra de
   título) e a navegação saiu da barra inferior para **abas roláveis no topo**
   (Virgin · Músicas · Álbuns · Artistas · Favoritas · Vídeos · Playlists), estilo web/Tami.
   Mini player virou **cápsula flutuante** no rodapé + **FAB de busca**. Tema planetário mantido.
   - `activity_main.xml` reestruturado (FrameLayout raiz + toolbar slim `bg_navbar` + `tab_strip.xml` + FAB)
   - `MainActivity` sem `BottomNavigationView`; tab padrão = `VirginHomeFragment`;
     `bindTab()`/`syncTabStrip()`/`syncToolbar()` novos
   - `menu_bottom_nav.xml` REMOVIDO (limite de 5 itens do Material não se aplica mais)
2. **Rádio Virgin/Victor**: nome do rádio da biblioteca segue o avatar (`Settings.assistantName`),
   na tela (`TamiRadioActivity`), no card da Home (`VirginHomeFragment`) e nas falas (`TamiRadio`).
   Rádio de estações (`RadioActivity`) intacto.
3. **Painel admin de banir (escondido)**: toque longo no avatar abre `BanActivity` — login por
   token (guardado em Keystore via `Settings.setAdminToken`), lista de dispositivos e
   Banir/Desbanir via `AdminApi` (`/admin/login`, `/admin/devices`, `/admin/ban|unban`).
   Entrada propositalmente não visível (quem não tem o token não faz nada).

### Verificado
- `./gradlew :app:assembleDebug` PASSED (build final da release)

## Convenções
- Commits de release: `Pulsa X.Y.Z (code): descricao; changelog pt/en/es` com bump de versionCode/Name
- Commits internos/CI: prefixo `ci:`/`refactor:` sem bump — CI pularia publicação (guarda anti-regressao)
- Build: `./gradlew :app:assembleDebug` (local ~2-7min conforme cache)

## Ensinamentos da sessão
- Style Android sem `parent` com nome `Widget.Pulsa.X` herda de `Widget.Pulsa` (inexistente) → AAPT
  "resource style/Widget.Pulsa not found". Sempre dar `parent` válido (ex.: `android:Widget.Material.TextView`).
- CI publica release automaticamente no push (`.github/workflows/build.yml`).
- Banco é feito no painel do servidor `pulsa-telemetry/admin.html` (ou agora no app via toque longo no avatar).