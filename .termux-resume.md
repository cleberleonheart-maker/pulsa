# Resumo · continuação da sessão (termux fechou)

> Este arquivo registra o estado do trabalho para retomar caso o terminal feche.
> Atualizado: 22/09/2026 · release **5.0.1 (107) COMMITADA e PUBLICADA via CI** (`559d031`).
> UpdateChecker já vê `107|5.0.1` no pulsaweb → o app oferece o update (sem root).

## Projeto
- Repo: `/root/pulsa` (branch `main`)
- App Android Kotlin/Gradle: `app/`
- Último commit: `Pulsa 4.14.0 (105)` — working tree limpo (release fechada)

## Release 4.14.0 "Terminal Radar" (o que foi feito)
Repaginação **completa** de identidade — o usuário pediu "cara nova, mudança completa", não só repintura:
1. **Paleta Terminal Radar** (colors.xml): lima `#B6FF2E`/`#8CE820` + rubi `#FF2E4D`/`#FF6A7A`
   sobre preto-esverdeado (`bg #06100B`, `bg_elev #0C1C13`, `surface #08150E`, `surface_variant
   #11291B`); sem mais rosa/roxo/ciano.
2. **Chassi chanfrado 45°** em toda a interface:
   - `themes.xml`: attrs `shapeAppearanceCorner*` e `shapeAppearance*Component` → style
     `ShapeAppearance.Pulsa.Radar` (parent `ShapeAppearance.Material3.Corner.Medium`,
     `cornerFamily=cut`, `cornerSize=10dp`); NeonButton 18dp → 10dp.
     **Atenção: `MaterialCardView` NÃO herda os attrs globais do tema → precisa
     `app:shapeAppearance` explícito em cada card.**
   - `values/attrs.xml` (styleable `RadarPanel`: panelColor/panelStroke/panelStrokeWidth/panelCut)
     + `RadarPanelDrawable.kt` (corte fixo em dp) para rows/inputs/decks/cápsulas.
   - Drawables reescritos: `bg_row_glass`, `bg_input`, `bg_mini_player`, `bg_bottomnav`,
     `bg_deck`, `bg_aurora` (novo céu de varredura: retículo, blips, grid, poeira estelar).
   - Layouts com shape Radar: item_album, item_song, mini_player, fragment_list, fragment_now_playing
     (hero), fragment_artist_timeline (2 cards), activity_dj (CardView → MaterialCardView — dados
     contexto único via ids `dj_art_a`/`dj_art_b`).
3. **Sweep de radar animado** nos anéis da reprodução: `OrbitRingsView.kt` repintado lima/rubi +
   `drawSweep` (wedge 42°, linha líder lima, blip rubi; só com `animated`).
4. **HUD de terminal**: `TextAppearance.Pulsa.Hud` (monospace, allCaps, letterSpacing) — rótulo
   "PULSA" na Home da Virgínia; sobretítulo `app_name` com Hud primary 12sp; btn_relax `#04140A`.
5. Gradientes/ícones refinados: virgínia/launcher/splash terminando em rubi `#C91F36`.

## Reforços do usuário (NÃO REMOVER)
- **A locutora** (Virgínia/DJ) — todo o ecossistema `DjVoice`/`DjCommander`/`MainVirgin`.
- **Curiosidade entre as músicas**: ela fala fatos reais sobre músicas/bandas entre as faixas
  (a cada ~5-10 min) — `DjFacts` + `PlaybackService` (função `curiosity*`, `fetchRemoteCuriosity`)
  + curiosidade também na timeline do artista.

## Estado atual
- **RELEASE 5.0.0 (106) FECHADA**: `fe9d719` commit + push → CI `build.yml` PASS (tests+assembleRelease)
  e publicou no pulsaweb (`version`: `106|5.0.0`). UpdateChecker do app vai oferecer o update.
- **Virgin FM + música favorita do avatar**: `AvatarFavorites` (mais tocada via `DjLearn.topSongs`,
  fallback aleatório) → abre o Rádio Virgin tocando a favorita primeiro e anunciando
  (`TamiRadio.start`); favorita aparece no deck (`hero_fav`, "♥ … — …").
- **Botão de saudação**: pílula neon `btn_greet` na home fala "Bom dia/Boa tarde/Boa noite"
  (`home_greet_day/after/night` + voz).
- Strings novas (pt/en/es): `tab_biblioteca`, `home_library_title`, `home_assistant_title`,
  `radio_fav_format`, `home_greet_*`.
- **Build `assembleDebug` PASSA** (último: 17:4x). Corrigidos no caminho: `nav_item_color`
  duplicado (já existia em `res/color/`), `xmlns:app` ausente no `fragment_virgin_home.xml`,
  `scrollX="0dp"`, `RadarSectorView` (removido `DEG`/`anyway`, tipo Float), 3 botões A/B
  (`np_ab_a/b/clear`) + fileira A/B no Now Playing (WIP do usuário), `MainActivity.refreshPlayerVisuals`
  → `NowPlayingActivity.current`, `Int.dp()` clash removido, `DancingVirginView.setForceMale` clash
  → propriedade com setter custom, ESCAPE DE APÓSTROFO em strings-en (`avatar\'s` — aapt2 do AGP
  rejeita sem escapada).
- **Casal dançando**: quando a favorita do avatar toca, Virgin+Victor aparecem juntos no Now Playing
  (`np_virgin_dance_b` + `forceMale`).

## Release 5.0.1 (107) — feita em 22/09 (`559d031`, CI green 5m59s, pulsaweb = `107|5.0.1`)
- **Home sem painel**: deck (`bg_deck`) removido do `fragment_virgin_home.xml`; avatar `hero_avatar`
  agora livre e inteiro (150dp) acima do HUD/nome/status/favorita — não corta mais a figura.
- **Cor violeta aurora**: os avatares (`virgin_avatar.xml`, `avatar_masculino.xml`) trocaram o
  verde-limão `#B6FF2E` por `#A78BFA` (corpo/olhos/sorriso/laço/antena/glow); halo verde → violeta;
  cabeça masculina terminou em `#7C62E0`; bochechas masculinas → rosa `#FF4A5E`; `bg_avatar` glow
  → violeta; `ic_launcher.xml`/`ic_splash.xml` idem (dourados `#FFE9A8`/`#FFD9A8` mantidos).
  O resto do skin (céu bg_aurora etc.) continua lima — só os AVATARES mudaram.
- **Update não aborta mais na 1ª**: `fetchVersion` 3s/1x → 10s/2x (600ms entre) no `UpdateChecker`;
  `downloadFromServer` dá 2ª chance à fonte oficial (duplica a URL vinda do /version) antes dos
  fallbacks; dedup de "já tentado" parou de ser marcado ANTES do download (era `attempted_code` em
  `handle()`) → agora `attempted_name` só é gravado quando o download CONCLUI (`markAttempted`),
  falha chama `clearAttempted` (UpdateService e caminho manual) para a checagem automática re-oferecer.
- `prefs(context)` helper novo em UpdateChecker (`pulsa_update`).

## Próximos passos
- **No celular**: abrir o Pulsa → detecta 5.0.1 e oferece o update. Agora a 1ª tentativa fria já
  deve passar (10s + retry); se ainda abortar uma vez, NÃO precisa refazer (2ª chance da fonte).
- IDEIAS.md: feitos = tocando agora, Virgin FM/favorita, saudação, casal dançando; o resto
  (queue, letras, mini visualizador, skin rápido, stats, favoritas do mês, adicionadas recentes)
  continua como ideia.
- Próxima release: verificar visual no aparelho e ajustar se preciso.

## Convenções
- Commits de release: `Pulsa X.Y.Z (code): descricao; changelog pt/en/es` com bump de versionCode/Name
- Commits internos/CI: prefixo `ci:`/`refactor:` sem bump — CI pularia publicação (guarda anti-regressao)
- Build: `./gradlew :app:assembleDebug` (local ~3-20min nesta máquina ARM64, depende do cache)

## Ensinamentos da sessão (novos)
- Corrigir tag de abertura E fechamento quando trocar `CardView` → `MaterialCardView` (AAPT exige).
- Hex com dígito a mais (ex.: `#1FFFF2E4D`) → AAPT "incompatible with attribute color".
- `RadarPanelDrawable` precisa do `inflate(Resources, XmlPullParser, AttributeSet)` (3-arg, API 23)
  além do 4-arg (API 24+) e do import de `com.pulsa.player.R`.
- Style Android sem `parent` com nome `Widget.Pulsa.X` herda de `Widget.Pulsa` (inexistente) → AAPT
  "resource style/Widget.Pulsa not found". Sempre dar `parent` válido.
- Nesta máquina o `compileDebugKotlin` é o gargalo (pode passar de 10 min) — não interromper o gradle.
- Layout XML usando `app:shapeAppearance` precisa de `xmlns:app` no raiz (senão "prefix unbound").
- `android:scrollX`/`scrollY` em XML exigem unidade (`0dp`, não `0`).
- Extension e função com mesmo nome+sigla (ex.: `Int.dp()` e `dp(Int)`) colidem no JVM — usar um só.
- Se recurso "não existe" no código do usuário (`np_ab_*`, `ab_needs_a`), procurar no repo antes de
  criar: `ab_needs_a`/`ab_clear` já existiam em pt (default), só faltavam em en/es.
- `pm install` no MIUI falha mesmo como root/shell (XSpace exige MANAGE_USERS/CREATE_USERS) e o
  `am` precisa de uid shell com permissão de pacote — instalar sempre via UpdateChecker do app.
- Em strings-en, apóstofro DEVE ser escapado (`\'`) — sem escape, aapt2 do AGP dá
  "Invalid unicode escape sequence in string".