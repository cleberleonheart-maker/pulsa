# Resumo · continuação da sessão (termux fechou)

> Este arquivo registra o estado do trabalho para retomar caso o terminal feche.
> Atualizado: 15/09/2026 · sessão em andamento (NÃO commitado ainda)

## Projeto
- Repo: `/root/pulsa` (branch `main`)
- App Android Kotlin/Gradle: `app/`
- Último commit: `1f6a55d` v3.58 (65) — Virgin com curiosidade remota + toca só/mistura/repete essa/mix dormir

## Estado atual (WORK IN PROGRESS — sem commit)
Bugs reportados pelo usuário e sendo corrigidos:

1. **Comando de voz "músicas repetidas" não funciona** → melhorar matching no DjCommander (feito#) + corrigir loop do microfone
2. **Comando "pendrive" não funciona** → idem
3. **Virgin ativa pausa a música e fica com bip do microfone direto** → redução de restart do reconhecedor + duck da música em vez de pausa
4. **DjActivity anuncia comandos de voz no diálogo mas "duplicates"/"pendrive" NÃO estão implementados lá** → implementar (handler vazio no `when`)

### 1 · Diálogo "Comandos de voz" modernizado (CONCLUÍDO, build PASSED 23m)
Arquivos novos:
- `app/src/main/res/layout/dialog_voice_commands.xml` (cabeçalho com avatar Virgin + lista rolável maxHeight 420dp + botão Neon fechar)
- `app/src/main/res/layout/item_command.xml` (linha glass: ícone em círculo gradiente + frase + descrição + seta)
- `app/src/main/res/drawable/ic_delete.xml` (lixeira)
- `app/src/main/res/drawable/ic_copy.xml` (duplicatas)

Arquivo alterado:
- `DjActivity.kt`: `showCommandsDialog()` reescrita — infla `item_command` por comando, divide a string em frase ("Virgi, ...") e descrição pelo separador `" — "`, ícone por comando. Import de `android.view.ViewGroup` adicionado.

### 2 · DjCommandListener.kt (PARCIAL/feito#: matching)
`DjCommander.action()`:
- duplicates agora casa: `repetid`, `repitid`, `repeti`, `duplic`, `copias`, `iguais`
- pendrive agora casa: `pendrive`, `pendr`, `pen drive`, `pen-drive`, `pen d`, `cartao`, `usb`, `memoria`
- ATENÇÃO: ramo `duplicates` vem DEPOIS de `repeat` ("repete essa"/"repete a musica"/"repetir essa"/"repita essa") — ordem correta mantida

### 3 · DjCommandListener.kt (feito#: reduzir bips)
- `minGapMs` 2000 → 3000
- erro transiente (NO_MATCH/SPEECH_TIMEOUT): cooldown 350ms → 1500ms
- criado `retryRunnable`; limpo com `removeCallbacks` em erro fatal

⚠ FALTA em DjCommandListener: remover o `retryRunnable`/restart pendente no `stop()`/`destroy()` (usar `mainHandler.removeCallbacks(retryRunnable)`).

## Próximos passos (PENDENTES)
1. [ ] `stop()`/`destroy()` do DjCommandListener: `mainHandler.removeCallbacks(retryRunnable)`
2. [ ] **PlaybackService.kt**: flag `micListening` + duck em vez de pausa
   - novo campo `@Volatile micListening`, `micDucked`
   - `focusListener`: `AUDIOFOCUS_LOSS_TRANSIENT` → se `micListening` duck 0.35f senão pause; `AUDIOFOCUS_GAIN` → `restoreVolume()` + resume se `pauseOnFocusLoss`
   - `fun setMicListening(on: Boolean)` baixa/restaura volume (0.35f)
3. [ ] **Playback.kt**: `fun setMicListening(on: Boolean) { service?.setMicListening(on) }`
4. [ ] **MainActivity.kt**:
   - `startVirgin()`: `Playback.setMicListening(true)`; `stopVirgin()`: `Playback.setMicListening(false)`
   - `onStop()`: também `setMicListening(false)`
   - `virginLastSpeechEndMs`: setar no onDone do `virginSpeak` (tempo real do fim da fala), remover set do `doResumeVirginListener`
5. [ ] **DjActivity.kt** (grande):
   - `startMic()`/`stopMic()`: `Playback.setMicListening(true/false)`
   - `resolveVoiceCommand()`: adicionar `"duplicates" -> findDuplicates()` e `"pendrive" -> readPendrive()`
   - replicar lógica (compacta) de MainActivity: launcher `duplicatesLauncher`, launcher `pendriveTreeLauncher`, `pendingVirginAction` no REQ_STORAGE, `findDuplicates/performDuplicates/completeDuplicates`, pendrive (scan/copy/readAudioTags/PendriveFile)
   - novos imports: DocumentFile, Uri, Environment, MimeTypeMap, ContentValues, MediaScannerConnection, MediaMetadataRetriever, Video, VideoLibrary
6. [ ] Compilar: `./gradlew :app:assembleDebug` (última build levou ~23min no ambiente)
7. [ ] Versionar/patch: ainda NÃO commitado; se terminar, bump versionCode/Name antes (conforme CI de publicação)

## Strings já existentes (usar)
- `dj_voice_dup_search/none/found/done/failed`, `dj_voice_pen_search/none/all_dups/confirm/copy_done/copy_done_dups/copy_failed/cancel`
- Todos já usados pelo MainActivity.

## Referência
- Estado anterior (v3.58 publicado) no histórico do arquivo; patch antigo em `/root/rascunho-pulsa.patch`