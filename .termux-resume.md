# Resumo · continuação da sessão (termux fechou)

> Este arquivo registra o estado do trabalho para retomar caso o terminal feche.
> Atualizado: 21/09/2026 · sessão em andamento (release 4.12.0 em preparação — commits aguardando seu OK)

## Projeto
- Repo: `/root/pulsa` (branch `main`)
- App Android Kotlin/Gradle: `app/`
- Último commit: `Pulsa 4.11.1 (100)`
- **Em andamento (não commitado)**: release 4.12.0 (code 101) — build normal passou no fim da sessão

## O que já foi feito na release 4.12.0 (não commitada)
1. **Redesign Pulsar Neon** (que você fez hoje): synthwave noturno — púrpura profundo,
   rosa elétrico como primary, ciano como acento, `bg_aurora` virou poente com grid em
   perspectiva; colors.xml + ~40 drawables + `AnimatedBackground` + fragments retintos.
   Ficaram 5 resquícios da paleta antiga que não foram mexidos: `bg_circle.xml`,
   `bg_input.xml`, `bg_segment.xml`, `bg_scanner.xml` e `OrbitRingsView.kt` (hex antigos).
2. **Pulsa Rewind** (`Perfil → Pulsa Rewind`): `RewindActivity` + `DjLearn.rewind`
   (top 10, horários, semana, meses, melhor dia, coruja) + strings pt/en/es + drawables
   novos (bg_bar/peak/deck/progress_mini/status_chip/seg_glow). Bugs corrigidos na sessão:
   - `firstTs`/`firstSongId` eram ignorados no card "Primeira" (mostrava top[0]) — agora usa `rw.firstSongId`
   - eixo dia-da-semana desalinhado (agregação começava 2ª e legenda no dom) — agora `(DAY_OF_WEEK+6)%7` = domingo primeiro
   - play "Top N" desabilitava todas as linhas se a #1 sumisse da biblioteca — agora por linha (`song == null`)
3. **Login com Google**: botão "Entrar com Google" no login (conta criada com o e-mail do
   Google, token verificado via `/tokeninfo`). **Requer configurar** `googleWebClientId`
   em `gradle.properties` (ou env `GOOGLE_WEB_CLIENT_ID`) — sem isso o botão fica oculto.
   `Account.setProvider/provider` novo.
4. **Fixes**: fade do som ambiente; guarda de buffer no `DjRecognizer`; MD5 com sinal
   (Last.fm/Lyrics/RemoteSync); `Playback` notifica na main thread; `PlaybackService`
   (repeatAll desliga repeatOne, pausa sem perda de foco não solta áudio, `SleepTimer`
   fim-de-faixa); diálogo de sono no NowPlaying; guards `isFinishing` no UpdateChecker;
   comando "virgí, sugere uma música" + "quem é você" (`MainVirgin`).

## Já commitado antes
- `4.11.1` · avatar flutua estilo TAMI + `4.11.0` hero avatar home · `4.10.0` fix crash + widget
- Docs de convenções em "Convenções" abaixo.

## Próximos passos (quando voltar)
1. Conferir o changelog pt/en/es (já escrito para 4.12.0) e o build final
2. `git add` + commit de release `Pulsa 4.12.0 (101)` (push publica via CI)
3. Ajustar os 5 resquícios da paleta antiga se quiser fechar o tema
4. Decidir se o Google login fica na release mesmo sem `googleWebClientId` configurado

## Convenções
- Commits de release: `Pulsa X.Y.Z (code): descricao; changelog pt/en/es` com bump de versionCode/Name
- Commits internos/CI: prefixo `ci:`/`refactor:` sem bump — CI pularia publicação (guarda anti-regressao)
- Build: `./gradlew :app:assembleDebug` (local ~3-20min nesta máquina ARM64, depende do cache)

## Ensinamentos da sessão
- Style Android sem `parent` com nome `Widget.Pulsa.X` herda de `Widget.Pulsa` (inexistente) → AAPT
  "resource style/Widget.Pulsa not found". Sempre dar `parent` válido.
- CI publica release automaticamente no push (`.github/workflows/build.yml`).
- Nesta máquina o `compileDebugKotlin` é o gargalo (pode passar de 10 min) — não interromper o gradle.
- `Strings.format("%02x", byte)` gera hex errado (byte com sinal em Kotlin) — usar `it.toInt() and 0xff`.