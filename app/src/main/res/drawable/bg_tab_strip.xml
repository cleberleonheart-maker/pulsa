<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Corpo da pílula (vidro) -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="22dp" />
            <solid android:color="@color/violet_deep_a8C" />
        </shape>
    </item>

    <!-- Contorno néon suave -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="22dp" />
            <stroke
                android:width="1dp"
                android:color="@color/aurora_violet_a2A" />
        </shape>
    </item>
</layer-list>